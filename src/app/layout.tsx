import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "LaunchPage AI - Generate Landing Pages Instantly",
  description: "Create conversion-ready landing pages from a simple description using AI. Generate headlines, sections, and CTAs optimized for lead capture, payments, or bookings.",
  keywords: "landing page, AI generator, conversion optimization, lead generation, marketing",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
